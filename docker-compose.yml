#version: '3.8'

services:
  frontend:
    container_name: k_radio_frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3006:3006"
    environment:
      - TZ=Asia/Seoul
    #volumes:
    #  - ./frontend:/frontend
    restart: unless-stopped
